{
  "name": "eyes-sdk-javascript1",
  "engines": {
    "node": ">=10.0.0"
  },
  "private": true,
  "scripts": {
    "setup": "npm i && node scripts/setup.js --install --link",
    "link": "node scripts/setup.js --link",
    "unlink": "node scripts/setup.js --unlink"
  },
  "devDependencies": {
    "eslint": "^6.6.0",
    "eslint-plugin-import": "^2.18.2",
    "eslint-plugin-mocha-no-only": "^1.1.0",
    "eslint-plugin-node": "^10.0.0",
    "eslint-plugin-prettier": "^3.1.1",
    "prettier": "^1.18.2"
  }
}
