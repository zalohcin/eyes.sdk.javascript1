'use strict';

/**
 * @readonly
 * @enum {String}
 */
const OSNames = {
  Unknown: 'Unknown',
  Windows: 'Windows',
  IOS: 'IOS',
  Macintosh: 'Macintosh',
  ChromeOS: 'ChromeOS',
};

Object.freeze(OSNames);
exports.OSNames = OSNames;
