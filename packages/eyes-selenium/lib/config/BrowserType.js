'use strict';

/**
 * @readonly
 * @enum {number}
 */
const BrowserType = {
  CHROME: 'chrome',
  FIREFOX: 'firefox',
  IE: 'ie',
  EDGE: 'edge',
};

Object.freeze(BrowserType);
exports.BrowserType = BrowserType;
