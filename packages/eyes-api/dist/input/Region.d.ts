import { Location } from './Location';
import { RectangleSize } from './RectangleSize';
declare type Offset = {
    left: number;
    top: number;
};
export declare type Region = (Location | Offset) & RectangleSize;
export default class RegionData implements Required<Offset & Location & RectangleSize> {
    private _x;
    private _y;
    private _width;
    private _height;
    constructor(region: Region);
    constructor(location: Location | Offset, size: RectangleSize);
    constructor(x: number, y: number, width: number, height: number);
    get x(): number;
    set x(x: number);
    getX(): number;
    setX(x: number): void;
    get y(): number;
    set y(y: number);
    getY(): number;
    setY(y: number): void;
    get left(): number;
    set left(left: number);
    getLeft(): number;
    setLeft(left: number): void;
    get top(): number;
    set top(top: number);
    getTop(): number;
    setTop(top: number): void;
    get width(): number;
    set width(width: number);
    getWidth(): number;
    setWidth(width: number): void;
    get height(): number;
    set height(height: number);
    getHeight(): number;
    setHeight(height: number): void;
}
export {};
